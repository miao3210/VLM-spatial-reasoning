from setuptools import setup, find_packages

setup(
    name="third-party",
    version="0.1.0",
    packages=find_packages(),
    description="Third-party utilities for spatial-reasoning project",
    python_requires=">=3.8",
)
